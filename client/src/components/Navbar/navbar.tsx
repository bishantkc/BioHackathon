import {Paper } from "@mui/material";

const Navbar = () => {
  return (
    <Paper id="navbar">
      <div id="dashboard" style={{marginLeft: "50px"}}>
        BioInfo Hub
      </div>

    </Paper>
  );
};

export default Navbar;
